services:
    tomato-downloader:
        build: .
        environment:
            DATA_DIR: /app/storage
            HOST: 0.0.0.0
            PORT: 8787
            WEB_ORIGIN: "*"
        ports:
            - "8787:8787"
        volumes:
            - ./storage:/app/storage
        restart: unless-stopped
